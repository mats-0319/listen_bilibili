name: listen_b
description: "一个听歌软件，使用B站作为音源。"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  # 基础库
  uuid: ^4.5.3
  path_provider: ^2.1.6
  provider: ^6.1.5+1
  permission_handler: ^12.0.3

  # 功能库
  webview_flutter: ^4.14.1
  webview_flutter_android: ^4.14.1
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/logo_256.png
    - doc/manual.md
    - doc/tech.md

  uses-material-design: true
